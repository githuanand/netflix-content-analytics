```bash
# 📊 Dataset Overview

## 1. Source
The dataset is sourced from [Kaggle Netflix Dataset](https://www.kaggle.com/datasets/shivamb/netflix-shows).

## 2. Files
- **raw/netflix_titles.csv** → Original dataset
- **processed/netflix_cleaned.csv** → Cleaned dataset used for analysis
- **sample/netflix_sample.csv** → Lightweight dataset for preview

## 3. Columns
- `show_id` → Unique ID for every title
- `type` → Movie or TV Show
- `title` → Title of the content
- `director` → Director of the content
- `cast` → Cast members
- `country` → Country of origin
- `date_added` → Date added to Netflix
- `release_year` → Release year
- `rating` → Audience rating
- `duration` → Movie duration / number of seasons
- `listed_in` → Genre categories
- `description` → Short description

## 4. Size
- **Raw dataset:** ~1MB
- **Cleaned dataset:** ~900KB
- **Sample dataset:** ~50KB

```
